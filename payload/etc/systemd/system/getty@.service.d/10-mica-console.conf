# The console is an option: no getty starts until it has installed /usr/bin/login.
# On the floor alone agetty would exec a login that is not there, and restart.
[Unit]
ConditionPathExists=/usr/bin/login
